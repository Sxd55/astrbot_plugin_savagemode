# Savage Mode 实机验收（v0.2.0）

前置：AstrBot ≥ 4.27。每步都有明确期望，能被证伪。

## 0. 安装与加载

1. 目录放进 `data/plugins/astrbot_plugin_savagemode/` → 重载插件（或重启）。
2. 日志期望：`Savage Mode (L0) loaded v0.2.0`。
3. 插件列表里应显示 **Savage Mode** 与头像图标（metadata 的 `display_name` 与 `logo.png`）。

## 1. 面板能用

1. WebUI → 插件 → Savage Mode → 面板。
2. 期望：头像 + 徽章（版本/注入中/约 N tokens/缓存自检）显示正常；
   三个卡片（基础设置、块库、实时预览）都在，无 JS 报错（F12 Console 干净）。

## 2. 块库增删改查 + 排序 + 预览

1. 「+ 新增块」写两条：
   - 块 1：`总则`，内容 `回答要简洁，不要写长篇大论。`
   - 块 2：`红线`，内容 `不确定就说不确定，不要编造。`
2. 期望：每输入一次正文，右侧/下方预览在约 0.4 秒内更新；字数与 tokens 实时变化；
   点 ↑↓ 能换序，预览顺序同步变化。
3. 用开关停用块 2：预览里立即消失，tokens 下降。
4. 点「保存块库」→ 提示已保存；刷新页面（F5）后内容仍在。
5. 数据文件应为 `data/plugin_data/astrbot_plugin_savagemode/prompts.json`，
   内容包含两个块与顺序（sort）。

## 3. 注入是否生效

1. 面板里「总开关」开，「调试日志」开，保存设置。
2. 找个会话发一句普通消息。
3. 期望：日志出现 `Savage Mode L0 injected N chars (block=xxxxxxxx, ...)`；
   用代理或 `preview` 指令确认 system prompt 开头是
   `<!-- savage-mode:L0:vxxxxxxxx -->` + `<rules> … </rules>`。

## 4. 缓存友好（关键）

1. 同一会话连发 **3 条**消息。
2. `/savagemode status` 或面板徽章期望：`缓存自检 稳定（3/3）`；
   面板「上次请求 system prompt」长度 3 次一致。
3. 反例验证：改一条块的文字并保存 → 再发一条，块指纹变化；
   连发两条后自检重新回到稳定。

## 5. 基础设置

1. 「注入位置」改 `append` → 保存 → 发消息：注入块出现在 system prompt 末尾；改回 `prepend`。
2. 「生效平台」填 `telegram` → 保存 → 在当前平台发消息：不注入（日志显示 skipped）；
   面板徽章仍显示配置的平台。清空后恢复全平台。
3. 「包裹标签」改成 `mode_rules` → 预览里的标签随之变化；留空则不包裹。
4. 「基础文本」填一句 → 预览中它排在所有块之前；清空后消失。

## 6. 幂等与关闭

1. 连发 3 条消息后，预览与代理里本插件标记只出现一份（不叠加）。
2. 关闭「总开关」再发消息：system prompt 里标记与正文都被清掉；
   面板徽章显示「已关闭」。

## 7. 指令权限

非管理员发 `/savagemode status` 应提示权限不足；管理员三条指令均正常。

## 8. 记录

把每步的实际结果（截图或日志行）记到这行下面：
